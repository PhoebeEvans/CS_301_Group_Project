<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
    </data>

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp">

        <CalendarView
            android:id="@+id/calendarView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/white"
            android:weekDayTextAppearance="@style/CalendarView_WeekDay"
            android:dateTextAppearance="@style/CalendarView_DateText"
            android:focusedMonthDateColor="@color/black"
            android:unfocusedMonthDateColor="@color/gray"
            android:selectedWeekBackgroundColor="@color/yellow"
            android:selectedDateVerticalBar="@drawable/custom_selector" />

        <Button
            android:id="@+id/reset_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Show All Events"
            android:layout_marginTop="16dp"
            android:padding="8dp"
            android:textColor="@color/white"
            android:background="@color/black" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="All Events"
            android:textSize="18sp"
            android:paddingTop="16dp"
            android:textColor="@color/black"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/events_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingTop="16dp"
            android:layout_marginTop="10dp"
            android:background="@color/gray"/>
    </LinearLayout>
</layout>
